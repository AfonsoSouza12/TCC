import { VinculoUsuarioProjeto } from './vinculo-usuario-projeto';

describe('VinculoUsuarioProjeto', () => {
  it('should create an instance', () => {
    expect(new VinculoUsuarioProjeto()).toBeTruthy();
  });
});
